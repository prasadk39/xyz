import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import GitHubInterface from './GitHubInterface.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GitHubInterface />
  </React.StrictMode>
);
